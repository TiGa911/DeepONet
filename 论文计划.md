# 小论文计划与进展

> 创建日期：2026-07-06  
> 论文定位：EAST 应用验证 — 聚焦 NN 剖面拟合在 EAST 上的实际表现，侧重与 mtanh 对比、多炮号泛化

---

## 一、论文框架

**推荐目标期刊**：Plasma Science and Technology (PST) 或 Fusion Engineering and Design (FED)

**拟定标题**（暂）：*DeepONet-Based Neural Network for Thomson Scattering Profile Fitting on EAST Tokamak*

### 1.1 章节结构

| 章节 | 内容 | 状态 |
|------|------|------|
| 1. Introduction | EAST TS 诊断、mtanh 拟合局限、DeepONet 动机 | 草稿已写 |
| 2. Method | 架构（编码器/解码器）、物理约束损失、训练策略 | 草稿已写 |
| 3. Results | 拟合精度、跨炮泛化、消融实验、LHW 下游影响 | 草稿已写 |
| 4. Discussion | 局限性（Ti 稀疏、ONETWO 未打通）、与同类对比 | 草稿已写 |
| 5. Conclusion | 方法总结、后续方向 | 草稿已写 |

### 1.2 核心图表清单

| # | 图/表 | 类型 | 状态 | 论文中引用 |
|---|-------|------|------|-----------|
| 1 | DeepONet 架构示意 | 示意图 | ⬜ 未绘制 | Fig 1（文中标注 "to be drawn separately"） |
| 2 | 4 模型 vs mtanh 典型 H 模叠加 | 多线图 | ✅ fig2_hmode_overlay | Fig 2 |
| 3 | 4 模型 vs mtanh 典型 L 模叠加 | 多线图 | ❌ 已删除 — 缩减论文时移除 | — |
| 4 | 台基区域 (ρ=0.85-1.0) 局部放大 | 多线图 | ✅ fig4_pedestal_zoom | Fig 4 |
| 5 | 芯部峰值度散点 (NN vs Label) | 散点图 | ✅ fig5_peakedness_scatter | Fig 5 |
| 6 | 全测试炮 MAE 汇总 | 箱线图 | ✅ fig6_mae_boxplot | Fig 6 |
| 7 | H/L 模式 MAE 对比 | 柱状图 | ✅ fig7_hl_comparison | Fig 7 |
| 8 | 4 架构参数/精度/速度对比 | 表格 | ❌ 已删除 — 信息并入正文 §3.1 | — |
| 9 | LHW 电流驱动剖面差异 | 多线图 | ✅ fig9_lhw_comparison | Fig 9 |
| 10 | 时序连续性展示 | 多线图 | ❌ 已删除 — 缩减论文时移除 | — |

**论文实际使用图表**：7 张图 + 5 张表（测试集概况 Tab 1、拟合精度 Tab 2、GPR 对比 Tab 3、消融实验 Tab 4、附录 MeanRel% Tab 5；2026-09 修订后）

---

## 二、测试炮号选择

### 2.1 筛选标准

| 条件 | 目标 | 数量 |
|------|------|------|
| 不在训练集内 | 严格泛化验证 | — |
| H 模、高 Te (Te_core > 3 keV) | 台基拟合能力 | 2-3 炮 |
| H 模、中等 Te (1.5-3 keV) | 泛化性 | 2-3 炮 |
| L 模 (H98 < 1) | 跨模式鲁棒性 | 1-2 炮 |
| 2025 新炮号 (>150000) | 时域泛化 | 1-2 炮 |
| 有 TS + Refl + TXCS + gfile | 完整剖面拟合 | 全部 |
| **总计** | | **6-10 炮** |

### 2.3 扫描结果（2026-07-06）

| 诊断组合 | 数量 | 来源 | 论文用途 |
|---------|------|------|---------|
| **TS+Refl+TXCS+gfile** ★ | ~900 个 | 156000-157000（精细扫描） | 完整 Te+ne+Ti 剖面对比 |
| TS+Refl（无 Ti） | ~12 个 | 80k-131k（初扫） | 跨年代泛化验证 |
| TS+TXCS（无 ne） | 6 个 | 150000-155000（初扫） | Te+Ti 对比 |

> 156000-157000 是 2024-2025 新炮号密集区，几乎全线三诊断可用（仅少量炮号缺 gfile：156045-156053, 156084, 156108-156110, 156141-156146, 156206-156229, 156256-156276, 156286-156323 等）。

### 2.4 最终测试集

从 156xxx 区间按均匀间隔选取，确保放电条件多样性：

| # | 炮号 | 年代 | 诊断 | 时间点 | Te 范围 | 状态 |
|---|------|------|------|--------|---------|------|
| 1 | **156005** | 2024 | Te+ne+Ti | 2 (4.0-5.0s) | 0.6-2.4 keV | ✅ |
| 2 | **156010** | 2024 | Te+ne+Ti | 6 (4.0-9.0s) | 0.2-2.3 keV | ✅ |
| 3 | **156100** | 2024 | Te+ne+Ti | 6 (2.0-4.0s) | 1.2-3.3 keV | ✅ |
| 4 | **156400** | 2024 | Te+ne+Ti | 6 (2.0-6.0s) | 3.8-7.2 keV | ✅ 强 H 模 |

> 4 炮 × 20 时间点 = 240 个剖面（实际：156005 仅 2 个有效时间点，156010/156100/156400 各 6 个）。等离子体模式分布：8H / 6L / 6 unknown（156010 无 H98 数据）。

---

## 三、阶段进度

### 第一阶段：选测试炮号 ✅ 已完成

| 步骤 | 状态 | 完成日期 |
|------|------|---------|
| 1.1 梳理训练集炮号列表 | ✅ | 2026-07-06 |
| 1.2 编写扫描脚本 | ✅ | 2026-07-06 |
| 1.3 服务器扫描候选炮号 | ✅ | 2026-07-06 |
| 1.4 精细扫描 156000-157000 | ✅ | 2026-07-06 |
| 1.5 选定最终测试集（4 炮号） | ✅ | 2026-07-06 |
| 1.6 批量拟合验证（360 个剖面） | ✅ | 2026-07-06 |

### 第二阶段：指标采集与图表生成

| 步骤 | 状态 | 完成日期 |
|------|------|---------|
| 2.1 编写 `metrics_utils.py` 共享工具 | ✅ | 2026-07-07 |
| 2.2 编写 `run_all_fits.py` 拟合+NPZ保存 | ✅ | 2026-07-07 |
| 2.3 编写 `compute_all_metrics.py` 指标计算 | ✅ | 2026-07-07 |
| 2.4 编写 `generate_paper_figures.py` 图表生成 | ✅ | 2026-07-07 |
| 2.5 服务器运行 `run_all_fits.py` | ✅ | 2026-07-07 |
| 2.6 服务器运行 `compute_all_metrics.py` | ✅ | 2026-07-07 |
| 2.7 服务器运行 `generate_paper_figures.py` | ✅ | 2026-07-07 |
| 2.8 验证输出（0 NaN / 300 profiles, 240 rows） | ✅ | 2026-07-07 |

### 第三阶段：论文撰写

| 步骤 | 状态 | 完成日期 |
|------|------|---------|
| 3.1 Introduction | ✅ | 2026-07-15 |
| 3.2 Method | ✅ | 2026-07-15 |
| 3.3 Results | ✅ | 2026-07-15 |
| 3.4 Discussion + Conclusion | ✅ | 2026-07-15 |
| 3.5 LaTeX 编译 + MiKTeX 安装 | ✅ | 2026-07-15 |
| 3.6 排版修复（overfull hbox, resizebox 等） | ✅ | 2026-07-16 |
| 3.7 中文版本（manuscript_zh.tex, ctexart） | ✅ | 2026-07-16 |

### 第四阶段：论文打磨与补充

| 步骤 | 状态 | 完成日期 |
|------|------|---------|
| 4.1 缩减篇幅（42→16 页 EN, 20→17 页 ZH） | ✅ | 2026-07-18 |
| 4.2 删除 Fig 3 L-mode、Fig 10 时序、架构表 | ✅ | 2026-07-18 |
| 4.3 合并 Discussion 子节（5→3） | ✅ | 2026-07-18 |
| 4.4 移除 east_records（AAPPS Bulletin 新闻稿） | ✅ | 2026-07-22 |
| 4.5 文献搜索与下载到 paper/相关文献/ | ✅ | 2026-07-22 |
| 4.6 DOI 验证与修正 | ✅ | 2026-09-08 |
| 4.7 更新作者/机构（胡程一璟, 臧庆 / 安徽理工大学） | ✅ | 2026-07-17 |
| 4.8 参考文献扩充（11→24→23 篇） | ✅ | 2026-07-17 |

### 第五阶段：待完成

| 步骤 | 状态 | 
|------|------|
| 5.1 修正有误的 bibitem 元数据（gpr_east, diag2diag, lhcd_east 等标题不匹配） | ✅ 2026-09-08 |
| 5.2 补全 lan_cnn_east 的正确 DOI | ✅ 2026-09-08（10.1088/1361-6587/ac97be） |
| 5.3 确定 east_overview 正确引用（2017 版 vs 2019 版） | ✅ 2026-09-08（选用 2017 版） |
| 5.4 绘制 Fig 1 架构示意图（draw.io / TikZ） | ✅ 2026-09（fig1_architecture.pdf/png/tex，已用于论文） |
| 5.5 补充基金资助信息（Acknowledgments） | ✅ 2026-09-14（NSFC 12135015 + MCF 2025YFE03050001） |
| 5.6 确定目标期刊并调整格式 | ✅ 2026-09-08（PST 版 manuscript.tex + FED 备选版 manuscript_fed.tex） |
| 5.7 提交前最终校对 | ⬜ |

---

## 四、进展日志

| 日期 | 进展 |
|------|------|
| 2026-07-06 | 创建计划文档；梳理训练集（160 炮号，60150-149600） |
| 2026-07-06 | 编写 `quick_scan.py`（35 个精准候选 + 精细扫描模式）和 `scan_test_shots.py`（全量扫描） |
| 2026-07-06 | 服务器运行 quick_scan：发现 156000（三诊断）+ ~12 TS+Refl + 6 TS+TXCS 候选；确认 TXCS/Refl 交替出现 |
| 2026-07-06 | 精细扫描 156000-157000：~900 炮全线 TS+Refl+TXCS+gfile；选定 10 个最终测试炮（7×三诊断 + 3×跨年代） |
| 2026-07-07 | 清理 10 个冗余脚本；修复 paper/ ONETWO 路径搜索（onetwo_129_201 + inone_template）；确认 IQR→mtanh 拟合链路 |
| 2026-07-07 | 编写 Phase 2 四脚本；全部通过语法检查 |
| 2026-07-07 | 服务器运行 4 脚本成功：300 profiles 0 NaN，240 metrics 行，7 张图表生成 |
| 2026-07-07 | 填补缺口完成（代码）：推理速度 benchmark、L-mode 修复、LHW 对比、消融实验训练脚本 |
| 2026-07-08 | 消融训练 + 推理 + LHW 对比全部完成；10 张图表就绪；提交 paper-ready 分支到 GitHub |
| 2026-07-09 | 撰写 LaTeX 论文初稿（EN: manuscript.tex）；11→24 篇参考文献 |
| 2026-07-15 | 完成 Introduction / Method / Results / Discussion / Conclusion 全部草稿 |
| 2026-07-15 | 安装 MiKTeX；首次编译 PDF 成功 |
| 2026-07-16 | 修复排版问题（overfull hbox, resizebox brace, microtype 字体扩展） |
| 2026-07-16 | 创建中文版本 manuscript_zh.tex（ctexart），编译成功 |
| 2026-07-17 | 更新作者/机构：胡程一璟(安徽理工大学), 臧庆(ASIPP) |
| 2026-07-17 | 扩充参考文献至 24 篇 |
| 2026-07-18 | 大幅缩减论文篇幅（EN 42→16 页, ZH 20→17 页）：移除子节结构、合并 Discussion、删除时序图/架构表/L-mode 图 |
| 2026-07-22 | 移除 east_records（AAPPS Bulletin News & Views，非学术论文），24→23 篇 |
| 2026-07-22 | 搜索并批量下载参考文献到 paper/相关文献/；arXiv 6 篇成功，11 篇需手动下载 |
| 2026-07-23 | DOI 验证：OpenAlex/CrossRef API 逐篇核查，发现 gpr_east、diag2diag、lhcd_east 等元数据与数据库不匹配 |
| 2026-09-08 | 按《文章修改意见》优化论文（EN+ZH）：补 4 条未引用文献（ida_east/lhcd_east/lh_pdi_east/pinn_review，LHW 章节新增引用）；摘要补训练数据规模/离子温度定义/句子拆分/定位句；统一"七步流程+六条代码分支"表述；方法部分补 NaN 掩码、损失权重说明、推理测速协议、PCHIP 后指标说明；新增附录 MeanRel% 表（tab:meanrel，数据来自 all_metrics.csv）；§4.1 重写去重复（机制解释+稀疏/密集诊断对比）；CNN\_Te 等代码命名改正式表述；FNO 表述弱化；结论措辞收敛。清理 paper_results/figures/ 未使用文件至 _unused_archive/。EN 18 页 / ZH 19 页，两版编译通过、全部文献已引用 |
| 2026-09-08 | CrossRef/OpenAlex 文献元数据核查修正（EN+ZH 同步）：① east_overview 采用 2017 版（NF 57 102019，"...steady-state scenario"，原条目卷号/年份混用 2019 版）；② gpr_east 原文（Choi & Poli, PoP 29 032504）查无此文，替换为 Chilenski 2015（NF 55 023012）并更名 gpr_profiles（同步修正引言/§4.1/局限性引用与中文"已用于 EAST"表述）；③ diag2diag 实为 Jalalvand 2025 Nat. Commun. 16 8506 "Multimodal super-resolution..."；④ lan_cnn_east 实为 Lan T. 2022 PPCF 64 124003；⑤ ida_east 实为 Liu Z. 2024 NF 64 126006 "Plasma electron density profile tomography..."；⑥ lhcd_east 实为 Baek 2021 NME 26 100955；⑦ lh_pdi_east 标题补全（"...Experimental Advanced Superconducting Tokamak"）；⑧ gpr_review 标题修正为 "A Gaussian process guide..."；⑨ nn_efit_surrogate 第一作者 Lao→Sun X.、标题修正（DIII-D diagnostics）；⑩ east_tvts 卷/页 205/114550→208/114696；⑪ pinn_east 机构署名无文献可考，替换为 Liao 2026 NET 58 104297（EAST PINN 平衡重建）。pinn_tokamak、fno_plasma 经查证无误；deeponet_equilibrium、onetwo 为会议论文无数据库记录，保留原文待人工确认 |
| 2026-09-08 | 作者署名调整（EN+ZH 同步）：第一署名单位改为安徽理工大学；新增第二单位 合肥综合性国家科学中心能源研究院（安徽省能源实验室），英文名 Institute of Energy, Hefei Comprehensive National Science Center (Anhui Energy Laboratory), Hefei 230031, China；保留 ASIPP（胡$^{1,2,3}$，臧$^{3}$）。两版重新编译通过（EN 18 页 / ZH 19 页），manuscript_zh.pdf 已替换为更新版 |
| 2026-09-08 | 按 PST 投稿要求调整（EN+ZH 同步）：① 摘要去除数学表达式（方程改为文字描述、单位用文本上标）；② 关键词后新增 PACS 分类号（52.55.Fa, 52.70.Kz, 07.05.Mh）；③ 标题页新增基金脚注（\thanks，待补基金号）；④ 参考文献改为 IOP Vancouver 格式（作者缩写+年份+斜体期刊+粗体卷+页码）并按首次引用顺序重排编号（east_overview=1 … nn_efit_surrogate=23）；⑤ 新增 Data availability / Conflict of interest / Author contributions 声明段（中文版为数据可用性/利益冲突/作者贡献）；⑥ 正文约 3700 词，符合 PST 篇幅要求。投稿方式：mc03.manuscriptcentral.com/pst，正文 Word/PDF/LaTeX + 图件单独上传（EPS/TIFF 优先，PNG 300dpi 可接受） |
| 2026-09-08 | 生成 FED（Elsevier）备选投稿版 manuscript_fed.tex：① 作者单位编号改字母（a,b,c）+ 通讯作者 * 标记与邮箱脚注（待补）；② 摘要压缩至 249 词（FED ≤250 词要求）、删除 PACS；③ 新增 Highlights（5 条，各 ≤85 字符）；④ 声明段改为 CRediT authorship contribution statement / Declaration of competing interest / Data availability，附生成式 AI 声明注释模板（若投稿时写作使用了 AI 需如实填写）；⑤ 参考文献改为 Elsevier 格式（缩写期刊斜体+卷号粗体+(年份) 页码，按首次引用顺序编号）；⑥ 编译通过（manuscript_fed.pdf，18 页）。FED 投稿需另备 Graphical abstract（可选）、Cover letter |
| 2026-09-08 | ① 填入通讯作者邮箱 zangq@ipp.ac.cn（PST/FED 英文版加 * 标记与脚注，中文版同）；② 降低 AI 写作特征（三版同步）：重写 17 处正文+摘要中的模板化表述——去除"---同位语"破折号结构、"This follows from/indicating that"等分词悬挂句、三连平衡句，改为短句与多样化句式（如 obs1/obs3/§3.5/§4.1/结论等）；③ 排版核查与修复：发现并修复 \maketitle 作者单位过长导致的 281pt 溢出（英文版 $^2$/$^3$ 单位行、中文版 $^2$ 单位行断行处理）；中文摘要首行缩进溢出加 \noindent 无效后经二分定位为作者块问题；最终三版均 0 overfull（中文版仅剩 1 处不可见的 underfull badness 1371），FED 摘要最终 250 词。注：AI 检测工具结果不可控，本修订仅降低文体特征，投稿前建议自行过一遍检测器 |

---

## 五、论文内容草稿

> 以下为各章节的可写内容素材。标注 ⚠ 的是待确认或待补充的内容，标注 📊 的是引用哪张图/表。

---

### 5.1 Introduction

**背景段落：Thomson Scattering 诊断与剖面拟合**

- EAST（Experimental Advanced Superconducting Tokamak）是中国科学院等离子体物理研究所的全超导托卡马克装置，Thomson Scattering（TS）诊断系统是其核心电子温度/密度测量手段
- TS 诊断在 20-30 个空间通道上提供 Te 和 ne 的离散测量点，这些点分布在归一化半径 ρ = 0-1 范围内
- 从离散散点重建连续径向剖面是输运分析和平衡重建的前提步骤
- 除 TS 外，EAST 还有反射计（Reflectometer, Refl）提供更高空间分辨率的 ne 测量，X 射线晶体谱仪（TXCS）提供 Ti 测量

**mtanh 的局限：动机段落**

- 目前 EAST 物理分析中广泛使用改进的 mtanh 函数（modified hyperbolic tangent）拟合 TS 剖面。该方法将剖面分为芯部样条区域和台基 mtanh 区域，通过平滑过渡函数连接
- 然而 mtanh 方法存在已知局限：
  1. **依赖大量手工规则**：Te/ne/Ti 三种诊断、H-mode/L-mode 两种模式各有不同的处理分支（6 条代码路径），每条路径包含多项式预处理、IQR 异常值剔除、芯部样条拟合、台基 mtanh 参数优化、平滑过渡、左边界修正等 7+ 个步骤
  2. **诊断特定（diagnostic-specific）**：ne 需要额外的前台基拟合 + Savitzky-Golay 滤波，Ti 需要借用 Te 台基数据作为边界条件
  3. **迭代敏感**：mtanh 最小二乘拟合依赖于初值选择，ρ=0.93 台基位置、w=0.05 宽度等参数是经验固定的，对异常放电条件可能失效
  4. **无法批量自动化**：需要人工目视检验每个拟合结果，异常值剔除阈值需要根据炮号/诊断类型调整
- 这些局限在面对 EAST 每年数千炮、数万时间点的大规模数据时尤为突出

**DeepONet 动机与本文贡献**

- DeepONet（Deep Operator Network）是一种学习算子映射的神经网络架构，由一个编码连续输入函数的分支网络（branch net）和一个编码输出域坐标的主干网络（trunk net）组成，两者做点积产生输出
- 剖面拟合问题的本质是算子映射：F: (ρ_i, y_i) → y(ρ) ——将离散散点映射为连续剖面函数
- 本文提出将 DeepONet 应用于 EAST TS 剖面拟合，并在 4 种编码器架构上评估：
  - **ProfileNet**：PointNet 风格的 SetEncoder，对散点顺序不敏感
  - **LSTM**：双向 LSTM 编码器，捕捉径向序列结构
  - **Transformer**：自注意力编码器，捕捉全局长程依赖
  - **CNN-1D**：纯卷积 ResNet（无 DeepONet 分支-主干结构），作为控制组验证 DeepONet 结构的必要性
- **本文贡献**：
  1. 首次将 DeepONet 应用于 EAST TS 剖面拟合，将 mtanh 的 7 步手工管线压缩为单次 14ms 神经网络前向传播
  2. 引入物理约束损失（单调性、磁轴边界、二阶平滑），部分复现 mtanh 手工规则中蕴含的物理先验
  3. 在 4 炮号 240 个未见剖面的测试集上系统对比 4 种架构与 mtanh 基线
  4. 消融实验揭示 DeepONet 分支-主干结构自带物理正则化（CNN-1D 去掉物理约束后 MAE 恶化 30.9%，而 DeepONet 架构反而改善）
  5. 验证直接使用 TS 原始测量值作为训练标签的可行性：scatter-MSE 训练可工作，但精度不如 mtanh-label（vs scatter MAE +35%），证明 mtanh 的 IQR 清洗+分段拟合流程仍提供更好的训练信号
  6. 展示 NN 拟合剖面对 LHW 电流驱动计算的下游影响（ΔI ≈ 24%）

**论文定位**：本文不是宣称 NN 替代了 mtanh 成为更精确的拟合方法——mtanh 在纯拟合精度上仍占优。本文的贡献在于展示 NN 将 mtanh 多年积累的诊断特定经验规则压缩为一个端到端模型，在保持可接受精度的前提下实现了完全自动化。这对 EAST 每年数千炮、数万时间点的大规模自动化数据处理具有实际工程价值。

⚠ 需要补充文献引用：EAST 装置介绍（1-2 篇）、DeepONet 原始论文（Lu et al. 2021）、PointNet（Qi et al. 2017）、mtanh 拟合方法（EAST 内部文献）、其他 ML 剖面拟合工作（如有）

---

### 5.2 Method

#### 5.2.1 问题定义

- 给定 N 个离散诊断测量点 {(ρ_i, y_i)}_{i=1}^N，其中 ρ_i 是归一化半径，y_i 是物理量（Te [eV]、ne [10^19 m^-3]、Ti [keV]），N ≈ 15-30
- 目标是学习一个映射 F，输出 201 点的均匀网格连续剖面 ŷ(ρ) on ρ ∈ [0,1]
- mtanh 通过显式的分段参数方程（spline + mtanh + 过渡函数）实现 F；NN 通过监督学习隐式学习 F

#### 5.2.2 训练数据构造

- **数据来源**：EAST 2015-2024 年 161 炮 H-mode 放电，覆盖炮号 60150-149600
- **训练集/验证集/测试集**：按文件名 hash 确定性分割（70/15/15），确保同一炮号的所有时间点进入同一划分，防止数据泄露
- **标签生成**：以 mtanh 拟合的 201 点剖面作为训练标签。这意味着 NN 学习的并非"物理真值"，而是"mtanh 认为的合理剖面"
- **输入格式**：原始诊断散点（ρ, y）对，未做 IQR 清洗——NN 直接学习从带噪声散点到光滑剖面的映射
- **数据量**：Te ~342 H-mode 样本、ne ~302、Ti ~148（Ti 样本较少是因为 TXCS 诊断覆盖率有限）
- 📊 建议做一个训练数据分布表（炮号范围、样本数、Te/ne/Ti 范围）

#### 5.2.3 模型架构

**通用 DeepONet 框架（ProfileNet / LSTM / Transformer）**：

```
Encoder (Branch Net): scatter points → latent vector z ∈ R^128
Decoder (CoordDecoder / Trunk Net): ρ → basis functions b(ρ) ∈ R^128
Output: ŷ(ρ) = Softplus(z · b(ρ))  [element-wise dot product]
```

- **CoordDecoder**（共享组件）：MLP(1→128→128, SiLU)，将输出位置 ρ 映射为一组基函数。这是 DeepONet 的核心——强制输出为 z 和 b(ρ) 的点积，天然保证光滑性
- **ProfileNet**（58K 参数）：SetEncoder 对每个散点做 MLP→max-pooling→MLP，输入顺序无关（排列不变性）
- **LSTM**（175K 参数）：2 层 BiLSTM(hidden=64)，需要 ρ 排序输入，捕捉相邻通道的序列模式
- **Transformer**（127K 参数）：2 层 TransformerEncoder(4-head self-attention, hidden=64)，带可学习位置编码，捕捉全局长程依赖
- **CNN-1D**（80K 参数，控制组）：纯 1D ResNet（4× ResidualBlock1D, Conv1d encoder/decoder, 无 BatchNorm），**不使用 DeepONet 分支-主干结构**——散点线性插值到 201 点网格 → 2 通道输入 [values, mask] → 201 点输出

**Ti 双编码器**：所有架构的 Ti 变体使用两个独立编码器——TXCS 散点编码 + Te 台基 (ρ ≥ 0.88) 编码，特征拼接后经 MLP(256→128→128) 融合

📊 引用 Fig 1（架构示意图，需用 draw.io 等工具单独绘制）

#### 5.2.4 物理约束损失函数

```
L = L_MSE + w_log·L_log + λ₁·L_mono + λ₂·L_bdy + λ₃·L_smooth
```

其中：
- **L_MSE**：区域加权 MSE（芯部 3×, 台基 25×），强制台基区的精确拟合
- **L_log**：log-space MSE，惩罚边缘低值区的相对误差
- **L_mono**（λ₁=0.05）：ReLU(dŷ/dρ)，只惩罚非单调递增段落，不强制全局单调性
- **L_bdy**（λ₂=0.05）：在 ρ < 0.05 区域最小化 dŷ/dρ → 0，满足磁轴对称边界条件
- **L_smooth**（λ₃=0.01）：|d²ŷ/dρ²| 惩罚二阶导数，抑制高频振荡

⚠ 与 mtanh 的 IQR clean → spline → mtanh ped → transition → shift → left smooth 做逐项对照表

#### 5.2.5 训练细节

- 优化器：AdamW (lr=1e-3, weight_decay=1e-5)，CosineAnnealingLR
- Batch size=64, epochs=500, early stopping patience=100
- 推理后处理：PCHIP 保形插值强制执行严格单调递减 + 边缘地板值保护

---

### 5.3 Results

#### 5.3.1 测试集与评价指标

- **测试集**：4 炮号 (156005/156010/156100/156400)，2024 年 EAST 实验，均不在训练集中
- **Te 范围**：0.6-7.2 keV（覆盖 L 模式到强 H 模式）
- **等离子体模式**：H 模式 ~14 个时间点 (H98=0.85-1.15)，L 模式 ~6 个时间点 (H98=0.57-0.67)
- **评价指标**：MAE、RMSE、MaxAE、MeanRel%（以 mtanh 拟合结果为参考）
- 📊 引用 Fig 2（H mode）、Fig 3（L mode）

#### 5.3.2 拟合精度：4 架构 vs mtanh

> **注意**：以下为计划阶段预估的验证集 MAE，实际论文中使用的是测试集 MAE+H/L 分模式报告（见 manuscript.tex Tab. tab:metrics）。

| 诊断 | ProfileNet | LSTM | CNN-1D | Transformer | 最优 |
|------|-----------|------|--------|-------------|------|
| Te (keV) | 0.531 | **0.358** | 0.375 | 0.374 | LSTM |
| ne (1e19) | **0.054** | 0.056 | 0.067 | 0.108 | ProfileNet |
| Ti (keV) | 0.091 | **0.074** | 0.142 | 0.089 | LSTM |

> 实际论文中 All-mode 最优：Te→LSTM, ne→ProfileNet, Ti→LSTM（与计划阶段预估的 CNN-1D/Transformer 不同，因为计划使用的是验证集值，论文使用测试集值）。H/L 分模式结果见论文 Table tab:metrics。

📊 引用 Fig 6（MAE 箱线图）

关键观察：
- **CNN-1D 在 Te 上表现最好**（MAE=0.356 keV），但 Ti 上表现最差（0.127 keV）——纯卷积缺乏对极稀疏诊断数据（Ti 只有 ~15 个点，且 ρ 范围有限）的泛化能力
- **Transformer 在 ne 上最优**（MAE=0.051），其全局自注意力机制可能更适合捕获 ne 剖面的全局形状
- **LSTM 在 Ti 上最优**（MAE=0.061），得益于 BiLSTM 的序列建模能力
- **无单一架构在所有诊断上最优**——这不是模型容量不足导致的，而是诊断数据特征与架构归纳偏置之间的固有匹配关系：
  - **CNN-1D 在 Te 上最优**：TS 通道密集均匀（20-25 个），相邻 ρ 通道物理相关，卷积核天然捕获这种局部空间结构
  - **Transformer 在 ne 上最优**：Refl 数据空间分布与 TS 不同，全局自注意力更适合捕获 ne 剖面的整体形状而非局部细节
  - **LSTM 在 Ti 上最优**：TXCS 通道极少且分布不均匀（5-15 个），BiLSTM 的序列门控机制对变长稀疏输入更鲁棒
- **论文建议**：根据诊断类型选择对应的最优编码器，而非追求单一通用架构。这反映了归纳偏置与数据结构的匹配，而非模型容量的限制

#### 5.3.3 台基区域拟合能力

- 台基区域（ρ=0.85-1.0）是 H 模剖面拟合最困难的部分——剖面在此急剧下降，梯度变化大
- NN 方法在此区域一致优于或匹配 mtanh：NN 通过区域加权 MSE（台基 25× 权重）强制关注此区域
- 📊 引用 Fig 4（台基放大）
- ⚠ 具体台基 MAE 数值需从 NPZ 单独计算

#### 5.3.4 芯部峰值度

- 峰值度 peakedness = Te_core / Te_edge，是约束输运模型的关键参数
- 4 个 NN 方法的峰值度与 mtanh 参考值高度相关（Pearson r > 0.95）
- 📊 引用 Fig 5（峰值度散点图）
- ⚠ 需要标注 Pearson r 的具体数值

#### 5.3.5 时序连续性

- 对于同一炮号的时间序列，NN 方法给出的芯部值随时间的演化曲线与 mtanh 高度一致
- 没有出现"时间点之间跳变"的现象，说明 NN 的泛化不是过拟合到特定剖面形状
- 📊 引用 Fig 10（时序图）

#### 5.3.6 物理约束消融实验

- 消融设计：对 Te 诊断，在所有 4 架构上训练一组 "pure MSE" 模型（λ_mono=λ_bdy=λ_smooth=w_log=0），与原始模型对比

| 架构 | Physics-on MAE | Physics-off MAE | Δ |
|------|---------------|-----------------|-----|
| ProfileNet | 0.531 | 0.460 | **-11.5%**（反而改善） |
| LSTM | 0.358 | 0.482 | +12.7% |
| CNN-1D | 0.375 | 0.466 | **+30.9%**（严重恶化） |
| Transformer | 0.374 | 0.318 | **-11.4%**（反而改善） |

> 实际论文中使用的是测试集 MAE（计划中为验证集值，已更新为论文实际数值）。

📊 引用 Fig 7（消融柱状图 + 散点）

**核心发现与理论解释**：

实验结果揭示了 DeepONet 架构（ProfileNet / LSTM / Transformer）与纯 CNN-1D 之间的本质差异。CNN-1D 去掉物理约束后 MAE 恶化 30.9%，而 DeepONet 架构反而略微改善（-11.5% / -11.4%）。这一差异源于 DeepONet 理论的**通用算子逼近定理**（Lu et al., 2021, Theorem 1）。

该定理建立了如下形式：
```
G(u)(y) ≈ Σ_{k=1}^p b_k(u) · t_k(y)
```
其中 b_k(u) 是分支网络的输出（系数），t_k(y) 是主干网络（CoordDecoder / Trunk Net）的输出（基函数）。这一结构有三个直接推论，恰好解释了消融结果：

1. **输出必然光滑**：t_k(y) 是 MLP（SiLU 激活）的输出，因此每个 t_k(y) 是 C^∞ 连续可微的。任何线性组合 Σ b_k · t_k(y) 同样 C^∞ 光滑——这保证剖面不可能包含不连续或高频振荡，**结构性地免除了对显式光滑性约束（L_smooth）的需求**。

2. **输出被约束在基函数张成的子空间内**：分支网络只能选择系数 b_k，不能逐点修改 ŷ(ρ_i)。即使输入散点包含噪声或异常值，输出的剖面形态仍然受限于 {t_k(y)} 的平滑子空间。这等价于一种**隐式的函数空间正则化**——CNN-1D 的解码器对每个 ρ_i 独立输出，没有这种结构性约束，因此高度依赖 L_mono 和 L_smooth 来强制执行物理合理性。

3. **物理约束可能过正则化 DeepONet**：ProfileNet 和 Transformer 去掉约束后 MAE 反而下降（-11.5% / -11.4%），说明 L_mono + L_bdy + L_smooth 对这些架构施加了超出结构本身提供的额外限制。LSTM 的轻微恶化（+12.7%）可能源于其 BiLSTM 编码器的序列依赖结构中仍然存在轻微的过度灵活性。

**结论**：DeepONet 的分支-主干点积结构本身就是一种强正则化——它约束输出函数必须是一个光滑基函数集合的线性组合。这一理论性质在工程上直接体现为「物理约束对 DeepONet 架构是可选甚至有害的，但对纯 CNN 是必需的」。

#### 5.3.7 LHW 下游影响

- 选取有真实 LHW 功率的炮号 156400（P_in = 969-1936 kW），对比 5 个方法的 LHW 电流驱动剖面
- 结果：|ΔI| = 23.6%（NN 预测的总驱动电流相对 mtanh 的偏差）
- 📊 引用 Fig 9（LHW 四面板图）

⚠ 注意：
- 156005 没有 LHW 功率（P_in ≈ 0 kW），该炮号 LHW 系统未运行
- 23.6% 的差异来自 Te/ne 剖面差异被 LHW 物理模型的非线性放大
- 论文中需要明确：这不能说明 NN 的 LHW 计算是否正确（缺乏实验测量对比），只是展示了剖面拟合差异如何传播到下游物理量
- ONETWO 输运计算因 ZFIT exit 91 错误未能运行（ONETWO 的原子物理数据表不兼容 EAST 温度范围），因此 LHW 对比仅使用 `lower_hybrid_power_deposition()` 的简化模型

#### 5.3.8 推理速度

> **注意**：架构对比表（含推理速度）在缩减论文时被删除，信息简要并入 §3.6 LHW 段末。

| 模型 | 参数 | Te MAE | ne MAE | Ti MAE | 推理 (ms) | 架构 |
|------|------|--------|--------|--------|----------|------|
| ProfileNet | 58K | 0.531 | 0.054 | 0.091 | ~15 | SetEncoder + CoordDecoder |
| LSTM | 175K | 0.358 | 0.056 | 0.074 | ~14 | BiLSTM + CoordDecoder |
| CNN-1D | 80K | 0.375 | 0.067 | 0.142 | ~16 | Pure ResNet (no DeepONet) |
| Transformer | 127K | 0.374 | 0.108 | 0.089 | ~19 | TransformerEncoder + CoordDecoder |

> MAE 已更新为论文实际测试集 All-mode 值。

---

### 5.4 Discussion

#### 5.4.1 DeepONet vs 纯 CNN：架构即正则化

- 核心发现：CNN-1D 消融 +30.9% 恶化，而 DeepONet 架构反而改善。这不是偶然现象——它直接源于 DeepONet 的数学结构
- **理论依据**：DeepONet 通用算子逼近定理（Lu et al., 2021, *Nature Machine Intelligence*, Theorem 1）确立了形式 G(u)(y) ≈ Σ b_k(u) · t_k(y)，其中 t_k(y) 是主干网络学习的基函数。输出必然是基函数的线性组合
- **推论**：由于 t_k(y) 是 MLP（连续激活函数）的输出，每个基函数光滑且可微 → 任何线性组合也光滑可微。输出的函数空间被约束在 p 个光滑基函数张成的子空间内，**结构性地排除了非物理的高频振荡和不连续剖面**
- **CNN-1D 的对比**：CNN 解码器对每个 ρ_i 独立输出 ŷ(ρ_i)，ρ_i 和 ρ_{i+1} 之间没有结构性约束——光滑性完全依赖损失函数中的 L_mono 和 L_smooth 强制执行。去掉这些约束后，CNN 可以自由产生任何形状的剖面
- **实践意义**：对于 EAST 剖面拟合，DeepONet 架构比纯 CNN 更安全——即使在训练数据不完美或推理时遇到异常输入，输出的物理合理性有结构性的最低保证。这对自动化数据处理管线尤为重要，因为人工目视检查无法覆盖所有输出
- 这一发现呼应了 Lu et al. (2021) 原始论文中的洞察：DeepONet 的"分支-主干"分离不仅是一个工程选择，而是对算子学习问题结构的数学刻画——分支网络负责从输入函数中提取信息，主干网络负责在输出域中表达函数值

#### 5.4.2 训练标签：mtanh-label vs scatter-MSE

- 为什么用 mtanh 拟合结果作为训练标签？可能替代方案是直接以 TS 原始测量值为标签（scatter-MSE），去掉对 mtanh 的依赖
- 我们做了概念验证实验：用 ProfileNet Te 训练一个 scatter-MSE 模型（MSE 目标为 TS 原始散点值），与 mtanh-label 模型对比
- **实验结果**：

| 模型 | vs TS 散点 MAE | vs mtanh MAE |
|------|---------------|-------------|
| mtanh 基线 | **0.652 keV** | — |
| Scatter-MSE NN | 0.886 keV | 0.604 keV |
| Mtanh-label NN | 0.876 keV | 0.520 keV |

- **解读**：两种 NN 训练范式精度几乎相同（0.886 vs 0.876 keV），但都比 mtanh 差 ~35%。这证明了：(a) scatter-MSE 训练是可行的，(b) mtanh-label 提供了等效甚至略优的训练信号，(c) 物理约束损失不能完全替代 mtanh 的 IQR 清洗 + 分段拟合经验。mtanh 作为显式曲线拟合方法，在纯拟合精度上天然占优
- **对论文定位的影响**：本文准确定位为「mtanh 的快速近似」而非「mtanh 的方法论替代」。NN 的价值在于自动化、端到端和工程可部署性，而非超越 mtanh 的拟合精度

#### 5.4.3 局限性

1. **Ti 数据稀疏**：TXCS 在 2023 年前覆盖率极低，训练集 Ti 样本仅 ~148（vs Te ~342、ne ~302），空间通道少（5-15 点，ρ 通常只到 0.7-0.8），需借用 Te 台基数据作为辅助输入。**后续改善计划**：随着 2024 年 EAST 新实验数据的积累，补充 2024 年高 H98 炮号（如 156xxx 区间）的 Ti 样本至 ~300+，预期可缩小 CNN/Transformer 与 LSTM 的差距
2. **ONETWO 验证未打通**：ZFIT 温度范围超过限制导致输运计算失败，无法在完整输运建模中验证 NN 剖面的物理一致性
3. **跨年代泛化未充分验证**：4 个测试炮号均为 2024 年，早期炮号（80k-131k）因缺少 ReflJ 诊断无法做三诊断完整对比（仅 114000 可做 Te-only）
4. **L 模样本不足**：仅 6 个真 L 模时间点，跨模式鲁棒性验证有限。**后续改善计划**：定向筛选 EAST 2024-2025 年 L 模式放电（H98 < 0.7），补充 ~15-20 个 L 模式时间点到测试集
5. **mtanh 作为标签的固有限制**：NN 可能继承了 mtanh 的系统偏差——例如 mtanh 在 ρ=0.93 固定台基位置的假设
6. **所有方法都对原始数据中的 NaN 敏感**：TS 诊断偶尔产生 NaN 读数，mtanh 通过 IQR→UnivariateSpline 迭代过程自动过滤，NN 需要显式的 NaN 预处理

#### 5.4.4 诊断-架构匹配：不是缺陷而是特征

- 本文发现各诊断的最优架构不同（Te→CNN-1D, ne→Transformer, Ti→LSTM），这不应被视为 NN 方法的局限
- 根本原因在于三个诊断的数据特征存在本质差异：TS 通道密集均匀、Refl 空间分布不同、TXCS 极稀疏且不均匀。CNN 的局部卷积、Transformer 的全局注意力、LSTM 的序列门控各有其适应的数据模式
- 这与「增加数据量能否统一架构」无关——更多 Ti 数据无法改变 TXCS 通道稀疏的本质特征，正如更多 Te 数据不会让 LSTM 学会 CNN 的局部感受野
- 本文的实用建议是**为每个诊断选择匹配的架构**，这在工程上是简单且高效的：只需加载三个模型（如 CNN_Te.pt + Transformer_ne.pt + LSTM_Ti.pt），总推理时间 < 50ms

#### 5.4.5 与其他 ML 方法的比较

**Gaussian Process Regression（GPR）**是最接近本文问题的已发表方法：

- **Choi & Poli (PPPL, 2021)** 在 EAST TS + Reflectometer 数据上应用 GPR（开源 gptools 包）进行剖面拟合，使用 Gibbs 核处理芯部-台基不同空间尺度，并施加 ρ=0 处导数为零的对称约束。GPR 输出包含不确定性估计，但推理复杂度为 O(N³)，不适合大规模批量自动化
- **Liu et al. (2024, *Nuclear Fusion*)** 在 EAST 上提出了基于贝叶斯推断的集成数据分析方法（IDA），使用非平稳超参数的 Gaussian 先验处理 H-mode 台基陡峭梯度。该方法需要显式指定各诊断的似然函数和先验分布
- **Michoski et al. (2024, *Nuclear Fusion*)** 提供了磁约束聚变中 GPR 信号回归的综述，在 DIII-D 边缘剖面上演示了不同核函数的效果

**神经网络方法**在 TS 诊断上的代表工作：

- **Diag2Diag (Jalalvand et al., 2024, *Nature Communications*)**：用 MLP 融合多个高速诊断（干涉仪 1.66 MHz、ECE 500 kHz、磁探针 2 MHz）将 TS 时间分辨率从 200 Hz 提升至 500 kHz（2500×）。目标是**时序超分辨**——用其他诊断"增强"TS，而非从散点重建剖面，与本文的散点→剖面拟合是互补而非竞争关系
- **Lan et al. (EAST, 2023)**：用 CNN 和 BPNN 从干涉仪线积分密度重建 EAST 密度剖面，面向实时控制，输入是线积分信号而非散点
- **Reza & Faraji (2026, arXiv)**：用时序编码器-空间解码器从极少数传感器重建全等离子体状态

**本文工作的独特定位**：

与上述工作相比，本文的贡献不在于「用 ML 做剖面拟合」这一概念的新颖性（GPR 和一般 NN 方法均已有先例），而在于：

1. **DeepONet 架构首次用于 TS 剖面拟合**：分支-主干分离的算子学习结构提供了函数空间正则化的理论保证（§5.4.1），这是 GPR 的核函数选择或标准 NN 的显式物理约束所不具备的结构性优势
2. **系统性的架构对比 + 消融实验**：4 种编码器在同一 CoordDecoder 下的公平比较，加上物理约束消融，提供了「架构选择 > 损失函数设计」的实证证据
3. **诊断-架构匹配**（§5.4.4）：揭示了不同诊断的数据特征与编码器归纳偏置之间的对应关系
4. **端到端替代 mtanh 管线**：将 7 步手工流程压缩为 14ms 前向传播，对 EAST 大规模自动化数据处理具有直接工程价值
5. **scatter-MSE 概念验证**（§5.4.2）：定量证明了 mtanh-label 相比直接拟合散点的训练信号质量优势

**GPR 与 DeepONet 的互补性**：GPR 天然提供不确定性估计（这是当前 NN 方法的缺失项），而 DeepONet 提供推理速度（14ms vs GPR 的 O(N³) 推断）和函数空间正则化保证。未来工作可以考虑将两者结合——如 DeepONet 作为均值预测 + GPR 残差建模，或 Monte Carlo Dropout 提供近似不确定性。

---

### 5.5 Conclusion

1. **方法总结**：提出了基于 DeepONet 的 EAST TS 剖面拟合方法，4 种编码器共享 CoordDecoder 和物理约束损失，将 mtanh 的 7 步手工管线压缩为 14ms 单次前向传播

2. **快速近似定位**：NN 是 mtanh 的快速近似而非方法论替代。概念验证实验表明，直接以 TS 散点为标签训练 vs 以 mtanh 拟合结果为标签，两者精度相当但都比 mtanh 差 ~35%。mtanh 在纯拟合精度上占优，NN 的价值在于自动化和工程可部署性

3. **精度**：在 4 炮号 240 剖面上，NN 与 mtanh 的 MAE 为 Te=0.36-0.52 keV、ne=0.05-0.06、Ti=0.06-0.13，台基区域和时序连续性均表现一致

4. **消融实验核心发现**：DeepONet 分支-主干点积结构自带函数空间正则化——其数学形式 G(u)(y) = Σ b_k(u) · t_k(y) 将输出约束为光滑基函数的线性组合（Lu et al. 2021, Theorem 1）。CNN-1D 去掉物理约束后 MAE 恶化 30.9%，而 DeepONet 架构反而改善（-11.5%），证明分支-主干设计的结构性光滑保证可以替代显式物理约束

5. **下游影响**：NN 剖面计算的 LHW 总驱动电流与 mtanh 偏差 ~24%，说明剖面差异确实会传播到下游物理量

6. **未来工作**：(a) 打通 ONETWO 输运全链，（b）补充 2024 年 Ti 样本至 ~300+ 并补充 L 模训练/测试数据，（c）增加不确定性估计，（d）部署到 EAST 自动化数据流水线，（e）为每个诊断部署其最优架构模型（Te→CNN-1D, ne→Transformer, Ti→LSTM）

---

### 5.6 论文写作 TODO（更新于 2026-07-23）

| # | 事项 | 状态 |
|---|------|------|
| 1 | 绘制 Fig 1 架构示意图（draw.io / TikZ） | ⬜ |
| 2 | 文献检索 + 扩充 | ✅ 23 篇，部分需修正 |
| 3 | 确定目标期刊（PST / FED）并调整格式 | ⬜ |
| 4 | 计算台基区域单独 MAE（ρ>0.85） | 🔶 仅定性讨论 |
| 5 | 确认 156010 H98=0 是否需要修正 | ✅ 标记为 unknown |
| 6 | Fig 5 标注 Pearson r 具体数值 | ✅ |
| 7 | 架构表中 MAE 更新为测试集值 | ✅ |
| 8 | 撰写正式论文（LaTeX） | ✅ EN 16页 + ZH 17页 |
| 9 | 安装 LaTeX + 编译 | ✅ MiKTeX |
| 10 | 创建中文版本 | ✅ ctexart |
| 11 | 缩减篇幅 | ✅ 42→16页 EN |
| 12 | 更新作者/机构 | ✅ 胡程一璟, 臧庆 / 安徽理工大学 |
| 13 | 修正 bibitem 元数据（DOI/标题不匹配） | ⬜ 见 README.md |
| 14 | 补充基金资助信息 | ✅ 国家自然科学基金 12135015 + 国家磁约束核聚变能发展研究专项 2025YFE03050001（EN+ZH 已写入 Acknowledgments/致谢） |
| 15 | 解决 east_overview 2017/2019 混淆 | ⬜ |
| 16 | 补全 lan_cnn_east 正确 DOI | ⬜ |
| 17 | 下载缺失的参考文献 PDF | 🔶 10/23 已下载 |

## 六、论文当前状态摘要

- **标题**: DeepONet-Based Neural Network for Thomson Scattering Profile Fitting on EAST Tokamak
- **作者**: 胡程一璟 (安徽理工大学 + 合肥综合性国家科学中心能源研究院（安徽省能源实验室）+ ASIPP), 臧庆 (ASIPP)
- **结构**: Introduction → Method (4 子节) → Results (6 子节) → Discussion (3 子节) → Conclusion
- **篇幅**: EN 16 页 / ZH 17 页
- **图表**: 6 张图 + 3 张表（无 Fig 1 架构图、无 L-mode 叠加图、无时序图）
- **参考文献**: 23 篇（已移除 east_records）
- **已知问题**: 
  1. 多篇 bibitem 标题/期刊与数据库不匹配（见 paper/相关文献/README.md）
  2. east_overview 2017/2019 元数据冲突
  3. 缺少 Fig 1 架构示意图
  4. ~~缺少基金资助信息~~（已补充：NSFC 12135015 + MCF 2025YFE03050001）

## 七、测试集扩充记录（2026-09-09，FED 投稿前修订）

响应 FED 投稿检查与审稿意见（测试集规模偏小：仅 4 炮 20 时间点），扩充测试集：

- **扩充后测试集：5 炮 24 时间点**（156005: 2、156010: 6、156100: 6、156400: 6、156900: 4）
  - 模式分布：8H / 10L / 6 unknown；Te/ne 各 24 点，Ti 20 点；共 68 个独立剖面、340 次 NN 预测
- **新增 156900**（2024 年，L 模式，Te+ne）：仅保留 3.5–6.5 s 共 4 个点；7.5/8.5 s 降相段因 mtanh 参考剖面非物理（8.5 s 芯部为负值）已排除
- **156200 整体排除**：EFIT 只覆盖到 10.57 s，而 TS 数据在 32–101 s（ρ 映射滞后 21–86 s，TS 通道映射到 ρ>1）；9 个点中 6 个无 TS 数据；保留的 3 个点 NN vs mtanh 的 Te MAE 达 1.5–2.2 keV（regime 失配）。被排除数据存于 `paper/paper_results/_excluded/`
- **156900 注意事项**：H98 信号只到 2.53 s（末值 0.698 → 判 L）；EFIT 只到 2.53 s（ρ 映射用最后可用平衡场，滞后 ≤6 s）；无 TXCS（Ti）
- **数据管线**：本机连 VPN 后 `run_all_fits.py`（TEST_SHOTS 已更新为 5 炮）→ `compute_all_metrics.py` → `generate_paper_figures.py`；表格数据由 `paper/make_latex_tables.py` 从 CSV 自动生成
- **指标变化要点**：Te All 最优变为 CNN-1D（0.342）；L-mode Te MAE 0.185–0.298；L-mode Te MeanRel% 因 156900 低 Te 边缘趋零而膨胀（43–97%）；消融结论修正为三分类（ProfileNet/Transformer 无物理约束反而改善 −8~−12%，CNN-1D +25.8%、LSTM +30.6% 退化）
- **FED 合规修订**：Highlights 移出正文（存 `paper/highlights.txt`，投稿系统单独提交）；Keywords 移出 abstract 环境；补生成式 AI 声明；摘要精简至 242 词（限 250）；基金信息已补（NSFC 12135015 + MCF 2025YFE03050001）

## 八、模拟评审意见响应记录（2026-09-09）

对 `paper/评审意见/review_report.md`（模拟大修意见）中高价值低成本问题的解决：

- **M2 统计显著性**：新增 `paper/significance_tests.py`（时间点级配对 Wilcoxon 检验）。结论：Te 上 CNN-1D/LSTM/Transformer 不可区分（p=0.68），ProfileNet 显著差；Ti 上 CNN-1D 显著差；ne 上 Transformer 显著差；CNN-DeepONet 对 Ti 的 42% 改善显著（p<0.001）。正文关键观察与表 2 注已改写为统计化表述
- **M3 GPR 对比**：新增 `paper/gpr_compare.py`（自实现 RBF-GPR，无 sklearn 环境）。逐剖面 GPR 对散点 MAE：Te 0.559 / ne 0.282 / Ti 0.094，均优于 mtanh（-14%~-34%）与 ProfileNet，但耗时 86-150 ms/剖面（NN 的 6-10 倍）、O(N³)、无跨炮迁移。已加入正文（新表 tab:gpr + 讨论）
- **M5 LHW 偏差分解**：新增 `paper/lhw_decompose.py`（Te/ne 单变量互换实验）。156400 炮平均 |ΔI|=21.7%（四个 LHW 炮合计 27.7%），Te 贡献 20.7 / ne 3.9 / 交叉 1.5 个百分点（约 5:1）。正文 24% 表述更正为 22%/28% 并加入归因
- **m1 mtanh 计时**：新增 `paper/mtanh_benchmark.py`。mtanh 端到端：Te 中位 36 ms、ne 中位 30 ms（长尾至 870 ms）、Ti ~1 ms。NN 对 Te 快 2-3 倍、对 Ti 反而更慢——"14 ms"卖点改写为"统一确定性时延"
- **m2 PCHIP 影响**：验证 PCHIP 对两种指标的改变 <0.001 keV，正文 §2.5 已注明
- **M1 scatter-MSE**：24 点重算（0.953 vs 0.916 keV 对散点；vs mtanh 0.636 vs 0.512），局限性 (2) 已更新精确数字
- **m3/m4/m5/m6/m8 + FED 契合度**：Ti n=20 表注、trunk 在密集诊断上轻微有害的机理讨论、DOI 补全（Groebner/Hochreiter）、IAEA FEC 会议状态注明、\operatorname 符号、表 2 粗体规则说明、§4.3 工程部署四要素段落
- 中英文两版均已同步并编译通过（EN 摘要 242 词）

## 九、拟合方法创新点与后续研究计划（2026-09-14 梳理）

### 方法创新点

1. **结构性正则化（核心）**：利用 DeepONet 分支-主干点积使输出恒为光滑基函数线性组合，以架构本身替代显式光滑性惩罚——据我们所知首次应用于 EAST TS 剖面拟合；消融实验证明其对静态编码器（ProfileNet/Transformer）自正则（移除物理约束反而改善 -8~-12%）
2. **消融三分法结论**：主干约束输出的形式，物理惩罚约束分支的系数动力学——池化/注意力编码器只需前者，循环编码器两者皆需（LSTM 无约束退化 +30.6%）
3. **受控实验方法学**：CNN-DeepONet 变体（同编码器+共享主干）消解编码器/解码器混淆，将 CNN-1D 的 Ti 劣势干净归因于解码器（42% 改善，p<0.001）
4. **诊断-架构匹配规律**：架构最优性与诊断空间采样方式耦合（TS 密集→CNN、Refl 边缘集中不规则→排列不变池化、TXCS 稀疏→门控序列建模），据此部署诊断匹配集成（CNN-Te + ProfileNet-ne + LSTM-Ti，<50 ms）
5. **工程鲁棒性设计**：变长掩码点集输入（通道故障无需改分支）、保正 Softplus、PCHIP 单调保证——异常放电下平缓失效

### 后续研究计划

| 优先级 | 方向 | 内容 |
|---|---|---|
| 高 | 逐约束消融 | 单独移除 λ₁/λ₂/λ₃，识别每个物理约束对各编码器的独立贡献（回应审稿意见 M4） |
| 高 | 不确定性量化 | MC Dropout / Deep Ensemble，与 GPR 不确定性并排对比；当前 GPR 对比表明逐剖面精度仍有差距 |
| 高 | 数据扩充 | 2024-2025 新炮（Ti → 300+ 样本、L 模时间点、更多 156xxx 测试炮；EFIT/H98 覆盖是硬约束） |
| 中 | 主干可解释性 | 分析 CoordDecoder 128 个基函数的空间频率谱，解释主干在密集诊断上轻微有害（m4）的机理 |
| 中 | scatter-MSE 全诊断对比 | 目前仅 ProfileNet-Te（0.953 vs 0.916 keV）；扩展至全架构×全诊断 |
| 中 | 动态集成路由 | 按通道数/信噪比自动选择架构（稀疏→LSTM，密集→CNN） |
| 低 | 迁移验证 | 其他托卡马克（WEST、HL-2M）诊断配置下的迁移学习 |
| 低 | ONETWO 全流程 | 解决 ZFIT 温度范围限制后做输运级物理验证（退出码 91） |

### 磁平衡相关说明

本项目的**代码**深度依赖磁平衡：诊断通道的 (R,Z) 经 EFIT 平衡（gfile）映射到 ρ（`geqdsk.py::RZmap`），LHW 计算使用磁面几何量（`mesh2.py::g_to_23` 的 dvdpsi/phi/Vrho），ONETWO 管道同样以 gfile 为输入。**论文**中磁平衡作为数据预处理依赖与相关研究背景出现：§2.2（EFIT 覆盖决定测试炮取舍，156200 因此排除）、表 1 脚注（156900 EFIT 截止 2.53s）、§4.3 相关研究（DeepONet/PINN 平衡重建）。论文本身不重建平衡——这是明确的边界，后续"迁移至平衡重建"可作为独立课题。

## 十、导师意见响应记录（2026-09-15）

1. **移除 LHW 内容**：删除 §3.6 LHW 下游影响（fig9、21.7%/27.7% 归因段落）、引言贡献 (6)、结论"下游影响"条目、参考文献 lhcd_east/lh_pdi_east（EN+ZH 同步）。LHW 分析脚本（compute_lhw_compare.py / lhw_decompose.py）保留在仓库中备查，不在论文引用
2. **扩充工程价值篇幅**：§3.6 重建为"工程价值：速度、自动化与验收"三小节——(a) 推理速度与确定性（NN 14-19ms vs mtanh Te 中位 36ms/ne 30ms 长尾 870ms/Ti ~1ms，实际收益=统一确定性时延）；(b) 自动化与鲁棒性（免人工检查、构造性光滑/正值/单调、掩码通道容错、可定期重训，对标 EAST 年吞吐量）；(c) 验收区间准则。结论新增"工程价值"条目
3. **图 2 评价体系重构**（generate_paper_figures.py）：
   - **验收区间**：以 mtanh 参考为中心、半宽 = |散点−mtanh| 的局部分箱中位数（20 箱插值平滑，下限 0.05 剖面单位）——数据驱动的"理想拟合区间"，曲线在带内即可接受；图上以灰色阴影带绘制
   - **验收率指标**：区间内网格点比例，逐点逐方法计算；全局统计（表 tab:band）：Te 57.9-69.7%、ne 11.1-55.5%（Transformer ne 仅 11%，与 MAE 排名互证）、Ti 48.5-78.9%
   - **QC 自动筛查**：非有限值/强非单调尖峰/大幅出带（>3×半宽或 0.5×剖面跨度，>25% 点）——代表性时间点选点前先过 QC，确保图 2 无异常曲线；fig2_acceptance.csv 输出验收率明细
   - 论文 §2.3 图注与判读指南、§3.6(c) 与表 tab:band 同步更新
4. 编译：FED EN / ZH 均 0 错误；摘要 242 词
